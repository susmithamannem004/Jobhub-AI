# Client environment variables
# Copy this file to .env.local for local development
# On Vercel, set these in Project Settings > Environment Variables

# Leave blank on Vercel (frontend and API share the same domain via rewrites)
# Set to your backend URL only when deploying frontend and backend separately
VITE_API_URL=
