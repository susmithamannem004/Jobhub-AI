# Server environment variables
# Copy this file to .env and fill in values

NODE_ENV=production
PORT=5000

# Required for production CORS — set to your frontend URL
CLIENT_URL=https://your-app.vercel.app

# Optional — leave blank to use the offline TF-IDF matcher
OPENAI_API_KEY=
